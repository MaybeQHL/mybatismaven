<?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE mapper
                PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.maybe.mybatis.mapper.UserMapper">
    <select id="findById" parameterType="int" resultType="User">
        SELECT * FROM user WHERE id=#{id}

    </select>
    <select id="findByUserName"  resultType="User">
        SELECT * FROM user
        <if test="_parameter != null">
             where username like #{'%_parameter%'}
      </if>

    </select>


    <select id="findUserList"    resultType="User">
        SELECT * FROM user

    </select>
    <insert id="insertUser" parameterType="User">
  insert into user (username,sex)
  values (#{username},#{sex})

        <!--
              将insert插入数据后生成的主键返回到user对象中
              SELECT LAST_INSERT_ID() 得到刚才插入的进去的记录的主键值，只能是自增主键
              keyProperty: 将查询到的主键值设置到这个parameterType指定的哪个属性
              order : 相对于sql语句来说的执行顺序
               -->
        <selectKey keyProperty="id" order="AFTER" resultType="int">
            SELECT LAST_INSERT_ID()
        </selectKey>
    </insert>

    <delete
            id="deleteUser"
            parameterType="int"
            flushCache="true"
            statementType="PREPARED"
            timeout="20">
         delete from user where id = #{id}
    </delete>

    <resultMap id="user_map" type="User">
      <!--主键-->
        <id property="id" column="id" />
        <result  property="username" column="username" />
        <result  property="sex" column="sex" />
    </resultMap>
    <!--'%${username}%'  concat('%',#{username},'%')-->
    <select id="getUserListMap" parameterType="User" resultMap="user_map">
        SELECT * FROM user
        <where>
            <if test="username !=null || username !='' ">
                username like  concat('%',#{username},'%')
            </if>
        </where>

    </select>

</mapper>